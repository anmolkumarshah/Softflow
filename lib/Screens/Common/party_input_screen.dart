import 'package:flutter/material.dart';

class PartyInputScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // TextFieldHelper(controller, labelText, inputType)
      ],
    );
  }
}
